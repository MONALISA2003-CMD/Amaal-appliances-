import Link from 'next/link'; import {ProductImage} from '@/components/catalogue/ProductImage'; import {notFound} from 'next/navigation'; import {getProduct,formatUGX} from '@/lib/catalogue'; import {AddToCartButton} from '@/components/commerce/AddToCartButton';
export default async function ProductPage({params}:{params:Promise<{slug:string}>}){const {slug}=await params; const p=getProduct(slug); if(!p)notFound(); return <main className="section"><div className="container"><Link className="back-link" href="/shop">← Back to shop</Link><div className="product-detail"><div className="detail-media">{p.image?<ProductImage src={p.image} alt={`${p.brand} ${p.model} product image`} brand={p.brand} model={p.model} width={900} height={700} style={{width:'100%',height:'100%',maxHeight:'480px',objectFit:'contain',objectPosition:'center'}}/>:<div><strong>{p.brand}</strong><span>{p.model}</span></div>}</div><div className="detail-copy"><div className="eyebrow">{p.category}</div><h1>{p.name}</h1><div className="detail-model">{p.brand} · {p.model}</div><p className="detail-description">{p.description}</p><div className="detail-price">{formatUGX(p.price)}</div><div className="detail-actions"><AddToCartButton product={p}/><AddToCartButton product={p} buyNow/><Link className="btn secondary" href="/cart">View bag</Link></div>{p.warranty&&<p className="warranty">Warranty: {p.warranty}</p>}<section className="spec-block"><h2>Specifications</h2><dl>{Object.entries(p.specifications).map(([k,v])=><div key={k}><dt>{k}</dt><dd>{v}</dd></div>)}</dl></section><section className="spec-block"><h2>Features</h2><ul>{p.features.map(f=><li key={f}>{f}</li>)}</ul></section></div></div></div></main>}