import Link from 'next/link';
export default async function OrderConfirmation({searchParams}:{searchParams:Promise<{order?:string}>}){const {order='AMA-REQUEST'}=await searchParams; return <main className="section"><div className="container confirmation"><div className="success-mark">✓</div><div className="eyebrow">Order request received</div><h1>Thank you for shopping with Amaal.</h1><p>Your order reference is <strong>{order}</strong>. The current frontend records the request locally; the production Amaal backend will take over payment, inventory, delivery and order persistence.</p><div className="hero-actions"><Link className="btn" href="/shop">Continue shopping</Link><Link className="btn secondary" href="/">Back home</Link></div></div></main>}
